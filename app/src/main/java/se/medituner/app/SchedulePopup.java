package se.medituner.app;
import java.util.*;
import java.time.*;

public static final LocalTime MIN;
public class SchedulePopup {


        static LocalTime time = new LocalTime.now();
        if(time.equals(12:00:00:00){
                questionPopup.showPopupWindow(currentScreen);
        }
}
